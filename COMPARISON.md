# D# vs C# - Сравнение синтаксиса

Этот документ показывает, как код на D# преобразуется в стандартный C# код.

## 🔵 Пример 1: Простые методы

### D# код (Example.ds)

```csharp
namespace DSharp.Sandbox.Generated;

public static class Example
{
    public static func HelloFromDSharp()
    {
        Console.WriteLine("Hello from D# language!");
        Console.WriteLine("This method uses 'func' instead of 'void'");
    }
    
    public static func PrintMessage(string message)
    {
        Console.WriteLine($"D# Message: {message}");
    }
}
```

### ⬇️ Сгенерированный C# код

```csharp
// <auto-generated/>
#nullable enable

namespace DSharp.Sandbox.Generated;

public static class Example
{
    public static void HelloFromDSharp()
    {
        Console.WriteLine("Hello from D# language!");
        Console.WriteLine("This method uses 'func' instead of 'void'");
    }
    
    public static void PrintMessage(string message)
    {
        Console.WriteLine($"D# Message: {message}");
    }
}
```

**Изменения:**
- `func` → `void`

---

## 🔵 Пример 2: Класс с методами

### D# код (Calculator.ds)

```csharp
namespace DSharp.Sandbox.Generated;

public class Calculator
{
    private int _result;

    public Calculator()
    {
        _result = 0;
    }

    public func Reset()
    {
        _result = 0;
        Console.WriteLine("Calculator reset to 0");
    }

    public func Add(int value)
    {
        _result += value;
        Console.WriteLine($"Added {value}, result = {_result}");
    }

    public int GetResult()
    {
        return _result;
    }
}
```

### ⬇️ Сгенерированный C# код

```csharp
// <auto-generated/>
#nullable enable

namespace DSharp.Sandbox.Generated;

public class Calculator
{
    private int _result;

    public Calculator()
    {
        _result = 0;
    }

    public void Reset()
    {
        _result = 0;
        Console.WriteLine("Calculator reset to 0");
    }

    public void Add(int value)
    {
        _result += value;
        Console.WriteLine($"Added {value}, result = {_result}");
    }

    public int GetResult()
    {
        return _result;
    }
}
```

**Изменения:**
- `public func Reset()` → `public void Reset()`
- `public func Add(int value)` → `public void Add(int value)`

---

## 🔵 Пример 3: Async методы

### D# код (AsyncExample.ds)

```csharp
namespace DSharp.Sandbox.Generated;

public static class AsyncExample
{
    public static async func DelayedPrint(string message, int delayMs)
    {
        Console.WriteLine($"Starting delayed print: '{message}' (delay: {delayMs}ms)");
        await Task.Delay(delayMs);
        Console.WriteLine($"Delayed message: {message}");
    }

    public static async func RunMultipleTasks()
    {
        Console.WriteLine("Running multiple async tasks with D# 'func' keyword...");
        
        var task1 = DelayedPrint("First", 100);
        var task2 = DelayedPrint("Second", 50);
        var task3 = DelayedPrint("Third", 150);
        
        await Task.WhenAll(task1, task2, task3);
        
        Console.WriteLine("All async tasks completed!");
    }
}
```

### ⬇️ Сгенерированный C# код

```csharp
// <auto-generated/>
#nullable enable

namespace DSharp.Sandbox.Generated;

public static class AsyncExample
{
    public static async Task DelayedPrint(string message, int delayMs)
    {
        Console.WriteLine($"Starting delayed print: '{message}' (delay: {delayMs}ms)");
        await Task.Delay(delayMs);
        Console.WriteLine($"Delayed message: {message}");
    }

    public static async Task RunMultipleTasks()
    {
        Console.WriteLine("Running multiple async tasks with D# 'func' keyword...");
        
        var task1 = DelayedPrint("First", 100);
        var task2 = DelayedPrint("Second", 50);
        var task3 = DelayedPrint("Third", 150);
        
        await Task.WhenAll(task1, task2, task3);
        
        Console.WriteLine("All async tasks completed!");
    }
}
```

**Изменения:**
- `async func` → `async Task` (не `async void`!)

---

## 📊 Таблица соответствий

| D# Синтаксис | C# Эквивалент | Примечание |
|--------------|---------------|------------|
| `func Method()` | `void Method()` | Обычный метод |
| `public func Method()` | `public void Method()` | С модификатором доступа |
| `private func Method()` | `private void Method()` | Приватный метод |
| `protected func Method()` | `protected void Method()` | Защищённый метод |
| `internal func Method()` | `internal void Method()` | Внутренний метод |
| `static func Method()` | `static void Method()` | Статический метод |
| `async func Method()` | `async Task Method()` | ⚠️ Важно: Task, не void! |
| `public static async func Method()` | `public static async Task Method()` | Комбинация модификаторов |

## 🎯 Ключевые особенности

### ✅ Что работает:
- ✔️ Все модификаторы доступа (public, private, protected, internal)
- ✔️ Static методы
- ✔️ Instance методы
- ✔️ Async методы (преобразуются в async Task)
- ✔️ Параметры любых типов
- ✔️ Вложенные классы
- ✔️ Generic классы и методы

### ❌ Что НЕ работает (пока):
- ❌ Методы с возвращаемым значением (только void/Task)
- ❌ Другие ключевые слова (только `func` реализован)
- ❌ Специальные операторы

## 🔍 Как посмотреть сгенерированный код?

После сборки проекта, сгенерированные файлы находятся в:

```
DSharp.Sandbox/obj/generated/DSharp.Generator/DSharp.Generator.DSharpSourceGenerator/
```

Пример:
- `Example.ds` → `Example.g.cs`
- `Calculator.ds` → `Calculator.g.cs`
- `AsyncExample.ds` → `AsyncExample.g.cs`

## 💡 Советы

1. **Используйте `func` для методов без возвращаемого значения** - это делает код короче
2. **`async func` автоматически становится `async Task`** - правильная семантика для async/await
3. **Сгенерированный код читаемый** - вы можете легко отладить его
4. **Добавьте свои преобразования** - отредактируйте `DSharpSourceGenerator.cs`

## 🚀 Примеры использования

```csharp
// В обычном C# коде можно использовать сгенерированные классы:
using DSharp.Sandbox.Generated;

// Вызов простого метода
Example.HelloFromDSharp();

// Использование класса
var calc = new Calculator();
calc.Add(10);
calc.Add(5);
Console.WriteLine(calc.GetResult()); // 15

// Async методы
await AsyncExample.RunMultipleTasks();
```

