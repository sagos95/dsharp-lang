# D# Quick Start Guide

## Что такое D#?

D# (D-Sharp) - это экспериментальное расширение языка C#, которое позволяет использовать альтернативный синтаксис при написании кода. Весь код на D# автоматически преобразуется в стандартный C# код во время компиляции с помощью Source Generator.

## Быстрый старт

### 1. Сборка проекта

```bash
dotnet build
```

### 2. Запуск песочницы

```bash
dotnet run --project DSharp.Sandbox
```

### 3. Создание своего .ds файла

Создайте файл с расширением `.ds` в проекте `DSharp.Sandbox`:

**MyCode.ds**
```csharp
namespace DSharp.Sandbox.Generated;

public static class MyCode
{
    public static func SayHello(string name)
    {
        Console.WriteLine($"Hello, {name}!");
    }
    
    public static async func WaitAndGreet(string name, int seconds)
    {
        Console.WriteLine($"Waiting {seconds} seconds...");
        await Task.Delay(seconds * 1000);
        Console.WriteLine($"Hello, {name}!");
    }
}
```

### 4. Использование в C# коде

**Program.cs**
```csharp
using DSharp.Sandbox.Generated;

MyCode.SayHello("Alexander");
await MyCode.WaitAndGreet("World", 2);
```

## Основные возможности

### ✅ Ключевое слово `func` вместо `void`

| D# | C# |
|----|-----|
| `public func Method()` | `public void Method()` |
| `public static func Method()` | `public static void Method()` |
| `private func Method()` | `private void Method()` |

### ✅ Async методы с `async func` → `async Task`

| D# | C# |
|----|-----|
| `public async func Method()` | `public async Task Method()` |
| `public static async func Method()` | `public static async Task Method()` |

## Структура проекта

```
DSharp/
├── DSharp.sln                          # Solution file
├── DSharp.Generator/                   # Source generator проект
│   ├── DSharp.Generator.csproj        # .NET Standard 2.0
│   └── DSharpSourceGenerator.cs       # Логика преобразования D# → C#
├── DSharp.Sandbox/                     # Проект-песочница
│   ├── DSharp.Sandbox.csproj          # .NET 9.0
│   ├── Program.cs                      # Точка входа
│   ├── *.ds                            # Ваши D# файлы
│   └── obj/generated/                  # Сгенерированные .g.cs файлы
└── README.md                           # Документация
```

## Просмотр сгенерированного кода

Сгенерированные C# файлы находятся в:
```
DSharp.Sandbox/obj/generated/DSharp.Generator/DSharp.Generator.DSharpSourceGenerator/
```

Например, для `Example.ds` будет создан `Example.g.cs`.

## Как это работает?

1. Вы пишете код в `.ds` файлах, используя D# синтаксис
2. При компиляции Source Generator (`DSharpSourceGenerator`) обрабатывает все `.ds` файлы
3. Генератор заменяет D# ключевые слова на C# эквиваленты:
   - `func` → `void`
   - `async func` → `async Task`
4. Генерируются `.g.cs` файлы с C# кодом
5. C# компилятор компилирует сгенерированный код вместе с остальным проектом

## Примеры из проекта

### 📄 Example.ds
Статические методы с использованием `func`

### 📄 Calculator.ds
Класс с методами и состоянием

### 📄 AsyncExample.ds
Асинхронные методы с `async func`

### 📄 Comprehensive.ds
Комплексный пример со всеми возможностями

## Советы по разработке

1. **IDE Support**: Используйте `.cs` подсветку синтаксиса для `.ds` файлов
2. **Debugging**: Отладка работает с сгенерированным C# кодом
3. **Errors**: Ошибки компиляции указывают на сгенерированные `.g.cs` файлы
4. **Hot Reload**: Изменения в `.ds` файлах требуют пересборки

## Расширение языка

Чтобы добавить новые возможности в D#, отредактируйте метод `TranslateDSharpToCSharp()` в файле `DSharpSourceGenerator.cs`.

Пример добавления нового ключевого слова:
```csharp
// Добавить замену "num" -> "int"
csharpCode = csharpCode.Replace("num ", "int ");
```

## Требования

- .NET 9 SDK
- Любая IDE с поддержкой C# (Visual Studio, VS Code, Rider)

## Лицензия

Это экспериментальный проект для изучения Source Generators.

